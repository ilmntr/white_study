# \, ', " 는 앞에 \를 붙여줘야 한다
# //는 소수점 버림
# ord() 문자를 아스키코드로
# chr() 아스키코드를 문자로
# 파이썬에서는 &&를 쓰지 않고 and 를 쓴다
# 비교할때 괄호도 해줘야 한다
# input은 str으로 받으니 정수와 비교할때는 int()를 해줘야 한다
# 2438    for문과 while문 while(not n==1) while()참일때 실행
# 10818    배열, numbers = list(map(int, input().split())) ,  range(0::) ,  print(min(numbers),max(numbers))min과 max
# 2439    print("*",end="")
# 10871    print(numbers[i],"",end="") ""는 띄어쓰기 하나이다
# 10950-1-2
# 1152    split 함수는 띄어쓰기단위로 한문자열을 나누어 리스트로 구분해준다        sen.insert(0,input())
# 2562    index(max(l)) index라는 샊            arr = list()        arr.append(int(input()))
# 2577   list(arr) arr이라는 배열을 한글자씩 분해    print(result.count(str(i))) count i라는게 몇개있는지 세어준다
# 2675    반복문, 형변환, len, 분해 등등
# 2908    문자 뒤집기,list기본 메소드
# 2920    arr = list(input().split()) 받은것을 띄어쓰기단위로 분해
# https://velog.io/@gillog/Python-List-%EA%B4%80%EB%A0%A8-method-%EC%A0%95%EB%A6%AC    list 메소드 정리
# 3052    arr를 set으로 집합 자료형으로 만들어준다  (set함수는 간단하게 말하면, 중복을 제거하기 위한 필터역할을 해준다.)
# 8958    ox퀴즈
# 10809    try: except:..Error     /에러가 나올땐 이케이케 하라






# https://www.youtube.com/watch?v=XSVswxqfWM8/    노래